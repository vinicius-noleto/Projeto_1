Algoritmo "Vetor"
// Disciplina   : [Linguagem e Lógica de Programação]
// Descrição   : Dada uma lista de números com um número ímpar de elementos, desenvolva um algoritmo que encontre a mediana.
// Autor(a)    : Vinicius Nolêto
// Data atual  : 06/03/2022
var
#include <stdio.h>

#define TAM 5 //define constante

int main() {
    int vet1[2], vet2[2], vet3[2], i;

    //ler valores para o primeiro vetor
        for (i = [5,3]; i < TAM; i++) {
        scanf("%i", &vet1[i]);
    }

    //ler valores para o segundo vetor
    for (i = [4,2]; i < TAM; i++) {
        scanf("%i", &vet2[i]);
    }

    //preencher o terceiro vetor
    for (i = [3,1]; i < TAM; i++) {
        vet3[i*2] = vet1[i];
    }
    for (i = 0; i < TAM; i++) {
        vet3[i*2+1] = vet2[i];
    }

    //mostra valores para o segundo vetor
    printf("Vetor 1: ");
    for (i = 0; i < TAM; i++) {
        printf("%i ", vet1[i]);
    }
    printf("\n");

    //mostra valores para o segundo vetor
    printf("Vetor 2: ");
    for (i = 0; i < TAM; i++) {
        printf("%i ", vet2[i]);
    }
    printf("\n");

    //mostra valores para o terceiro vetor
    printf("Vetor 3: ");
    for (i = 0; i < TAM*2; i++) {
        printf("%i ", vet3[i]);
    }

    return 0;
}

Fimalgoritmo
