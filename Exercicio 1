Algoritmo "Mediana"
// Disciplina   : [Linguagem e Lógica de Programação]
// Descrição   : Dada uma lista de números com um número ímpar de elementos, desenvolva um algoritmo que encontre a mediana.
// Autor(a)    : Vinicius Nolêto
// Data atual  : 06/03/2022
var
 //1) entrada de valores (5 números)
 //2) calcular a média

using namespace std;

int main()
{
    int vet[] = {1, 2, 3, 4, 5, 7}, soma = 0;
    float media;
    for (int i = 0; i < 5; i++) {
        soma = soma + vet[i];
    }
    media = soma / 5.0;
    cout << "Soma: " << soma << endl;
    cout << "Media: " << media;
    return 0;
}
Fimalgoritmo
